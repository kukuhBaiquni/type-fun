import { Canvas, Meta, Story } from "@storybook/addon-docs";
import Headers from "components/sections/header";

<Meta component={Headers} title="Components/sections/left-navigation/header" />

export const Template = () => <Headers />;

# Header

📓 Author: Kukuh Baiquni | <kukuh.baiquni@gmail.com> <br/>
⏰ Documented at: Monday November 29, 2021 - 13:55

Global `header` component, used for displaying theme toggler, main page navigation & user status.

File Path: **`/components/sections/header.tsx`**. <br/>
Used by: **Any layout components in `/components/layouts/*`**.

Notes:

- This component contain theme toggler to change appearance of website.
- This component only used by **`/components/layouts/*`**.
- When you toggle theme, another component affected by theme toggler will change their appearance, based on theme mode.

<Canvas>
  <Story name="header">{Template.bind({})}</Story>
</Canvas>

### 📝 **Changelog**

- _no recent changes_
